<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tarefas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tarefas">
        <activity
            android:name=".DetalheTarefa"
            android:label="@string/title_activity_detalhe_tarefa"
            android:theme="@style/Theme.Tarefas.NoActionBar"></activity>
        <activity
            android:name=".AdicionarPasta"
            android:label="@string/title_activity_adicionar_pasta"
            android:theme="@style/Theme.Tarefas.NoActionBar" />
        <activity android:name=".AddTarefas" />
        <activity android:name=".tarefas" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>